export default {
  // Always add semicolons for consistency
  semi: true,
  // Use single quotes for strings (JS community standard)
  singleQuote: true,
  // Add trailing commas for cleaner git diffs
  trailingComma: 'es5',
  // 2 spaces for readable indentation
  tabWidth: 2,
  // Line length limit before wrapping
  printWidth: 100,
  // Spaces inside object braces: { foo: bar }
  bracketSpacing: true,
  // Always wrap arrow function params: (x) => x
  arrowParens: 'always',
  // Unix line endings for cross-platform consistency
  endOfLine: 'lf',
};
